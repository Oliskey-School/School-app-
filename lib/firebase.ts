// Firebase configuration for Push Notifications
// MOCKED: Firebase is not installed in package.json, so we mock this to prevent build errors.

const app: any = null;
const messaging: any = null;

export { app, messaging };
